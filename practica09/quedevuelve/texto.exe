function test1(x, y) {
  return y - x
}

test1(10, 40)
este devolvera una resta.

function test2(x, y) {
  return x * 2
  console.log(x)
  return x / 2
}

test2(10)
este devolvera la multiplicacion que seria 20, porque el codigo ya se corto y mostrara en pantalla con el console.log(x)
